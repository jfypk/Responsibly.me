import React, { Component } from 'react';
import './Header.css';

class Header extends Component {
    render() {
        return(
            <header className="Inform-header">
                <h1 className="Inform-title">Inform</h1>
            </header>
        );
    }
}

export default Header;