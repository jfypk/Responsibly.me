to send a message from background:

let msg = {
    txt: "hello"
}

chrome.tabs.sendMessage(tab.id, msg);

or chrome.runtime.sendMessage(message); 
and chrome.runtime.onMessage.addListener(callback(request, sender, sendResponse))